<html>
<body>

<!-- 데이터 정의 -->
<% var title = 'EJS 예제' %>  

<h1> <%= title %></h1>

<h3>if 문</h3>
<div>
    <% if ( count ) { %> 
        num의 값은 <%= count %> 그리고

        <!-- if 조건문 -->
        <% if ( count % 2 == 0 ) { %>
            짝수
        <% } else { %>
            홀수
        <% } %>
    <% } else { %>
        count 값 없음.
    <% } %>
</div>

<h3>For 반복문</h3>
<ul>
   <!-- for 반복문 -->
   <% for ( var i = 0 ; i < count ; i++) { %>
    <li>
        <a href="./page/<%= i %>"> <%= i %> </a>
    </li>
    <% } %>
</ul>
<h1>태그와 어트리뷰트에서 사용</h1>
<%
    const tag = 'h3'
    const data = {title:'Title', link:'./link'}
%>
<<%= tag %>> TAG 이름에 사용  </<%= tag %>/>


<h3>링크의 어트리뷰트 값으로 사용</h3>
<div>
    <!-- 어트리뷰트의 값으로 사용 -->
    <a href="<%= data.link %>">link1</a>
</div>

<h3>Sports</h3>
<div>
    <a href="/sports">Sports</a>
</div>

</body>
</html>